from app import app as application 
# Replace 'app' with the name of your main Flask file (e.g., if your main file is 'run.py', use 'from run import app as application')

if __name__ == "__main__":
    application.run()